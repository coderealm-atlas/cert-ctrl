collections:
  - name: ansible.posix
    version: 2.2.2
  - name: ansible.windows
    version: 3.8.0
  # The generic package module dispatches to community.general.pkgng on FreeBSD.
  - name: community.general
    version: 13.4.0
  # Transitive dependency of community.general; pin it for reproducibility too.
  - name: community.library_inventory_filtering_v1
    version: 1.1.5
